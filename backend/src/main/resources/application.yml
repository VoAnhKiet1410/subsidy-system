spring:
  application:
    name: tro-cap-backend

  data:
    mongodb:
      uri: mongodb://localhost:27017/trocap
      auto-index-creation: true

  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

server:
  port: 8080

# JWT Configuration
jwt:
  secret: TroCapXaHoi2024SecretKeyForJWTTokenGenerationMustBeLongEnough256Bits!!
  expiration: 86400000  # 24 hours in milliseconds
  refresh-expiration: 604800000  # 7 days in milliseconds

# CORS
cors:
  allowed-origins: http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:3000

# Swagger
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

# File Upload
spring.servlet.multipart:
  max-file-size: 10MB
  max-request-size: 50MB

app:
  file:
    upload-dir: uploads
